import { getToken } from '../../styles';

export default {
  container: {
    marginTop: 50,
    color: getToken('color.success'),
  },
  bigBlue: {
    color: 'blue',
    fontWeight: 'bold',
    fontSize: 30,
  },
  red: {
    color: 'red',
  },
};
